Collection

List                                              |
-배열과 가장 유사한 성질을 갖고 있다.   |
-중복(o),순서(o)                               |
-첨자를 사용하여 접근                       |    C : (생성,입력,추가) add, map =put
Set                                               |    R : (읽기,검색) list=get(index),
-중복(x),순서(x)                                |     U : (수정)     
Map                                             |     D : (삭제) remove
-key,value가 한쌍으로 이루어짐            |
-key 중복불가. value는 중복가능           |
                                                   |
Properties                                       |
-key=value가 한쌍으로 이루어짐.           |
-key,value가 모두 문자열                     |

window builder

window builder 다운
뉴 어덜 윈도우 빌더 해서 생성 
디자인모드로 디자인한다


삭제할데이터는 삭제할떄 떙겨저서 리스트가 재정비되는데 앞에서부터 하게되면 리스트의 크기는 가변되지만
루프를 할떄의 리스트 사이즈는 고정이므로 크기값 오류가나고
비교를 아예 안하는 경우가 생길수 있다
그러므로
삭제를 할때는 루프를 뒤에서부터 앞으로 돌리는게 훨씬더 안전하다

내가 입력한 것이 포함됫는지에 대한 포함값을 알고 싶다면 indexOf로 비교한다
indexOf는 0이상이면 찾은게된다.
DB 셀렉트의 like랑 비슷한 유형